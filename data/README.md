# Content data

Everything a client will ever edit lives here. **All entries marked `[placeholder]`
must be replaced with real data before launch.**

Still outstanding: `locations.json` (realistic but unverified entries).

Confirmed and live: `visits.json` (the 2026 visit dates), and every contact
field in `site.ts`. One number, `+91 81695 23196`, answers both the phone and
WhatsApp; `whatsappHref` needs the digits alone, with no plus sign and no
spaces.
